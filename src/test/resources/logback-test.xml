<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Silence verbose Hibernate SQL/binder output in tests -->
    <logger name="org.hibernate.SQL" level="OFF" />
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="OFF" />
    <logger name="org.hibernate.type.descriptor.sql" level="OFF" />
    <!-- Silence additional Hibernate components that may print SQL/DDL -->
    <logger name="org.hibernate.engine.jdbc.spi.SqlStatementLogger" level="OFF" />
    <logger name="org.hibernate.tool.schema.internal" level="OFF" />

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>
